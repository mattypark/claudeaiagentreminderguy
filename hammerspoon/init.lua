-- Hammerspoon config

claudepet = require("claudepet")

-- Lets scripts drive the pet, e.g.
--   osascript -e 'tell application "Hammerspoon" to execute lua code "claudepet.test()"'
hs.allowAppleScript(true)
